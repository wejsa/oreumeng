{
  "name": "oreumeng-cms",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "migrate": "node scripts/migrate.mjs",
    "preview:cms": "node scripts/preview.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "sharp": "0.34.3"
  },
  "engines": {
    "node": ">=20"
  }
}
